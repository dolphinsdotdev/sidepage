"""Package data — bundled default PWA icons (`icon-192.png`, `icon-512.png`),
loaded via `importlib.resources.files("sidepage.assets")` from
`sidepage.core.pwa`. No code here; this file exists only so the directory
is an importable package `importlib.resources` can address by name.
"""
